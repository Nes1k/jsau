const INITIAL_STATE = [
  [null, null, null],
  [null, null, null],
  [null, null, null]
];

export default (state = INITIAL_STATE, action) => {
  switch(action.type){
    default:
      return state;
  }
};
